import Clusterer from "./layout/Clusterer";
import Optimizer from "./layout/Optimizer";

const tgview = {
    layout: {
        Clusterer: Clusterer,
        Optimizer: Optimizer,
    }
}

export default tgview;